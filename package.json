"homepage": "https://fiskey260.github.io/fiskey-pwa",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist",
  // ... other scripts like "start", "build" should already be there
}
